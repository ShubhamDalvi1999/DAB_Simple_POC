# The main pipeline for poc_dab_walkthrough (Serverless DLT version)

resources:
  pipelines:
    poc_dab_walkthrough_etl_single_node:
      name: poc_dab_walkthrough_etl_single_node
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/dab_walkthrough_etl"

      libraries:
        - glob:
            include: ../src/dab_walkthrough_etl/transformations/**

      configuration:
        # Pipeline-level configuration for dependencies
        bundle.sourcePath: ${workspace.file_path}
